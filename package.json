{
  "name": "irremarkables",
  "private": true,
  "version": "0.1.0",
  "description": "Irremarkables — a 3rd-person open-world simulated-multiplayer superhero betting game (PWA)",
  "type": "module",
  "scripts": {
    "dev": "node scripts/gen-font.mjs && vite",
    "build": "node scripts/gen-icons.mjs && node scripts/gen-font.mjs && vite build && node scripts/check-dist.mjs",
    "preview": "vite preview",
    "icons": "node scripts/gen-icons.mjs",
    "check": "node scripts/check-dist.mjs"
  },
  "dependencies": {
    "@fontsource/bangers": "^5.3.0",
    "three": "^0.166.1"
  },
  "devDependencies": {
    "vite": "^5.4.11"
  }
}
